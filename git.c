#include <stdio.h>
#include <stdlib.h>

int main()
{
   char s_name[20],area;

   float b_salary,sal;
   int serv,sales;
   printf("Enter your name : ");
   scanf("%s",&s_name);
   printf("Enter the area : ");
  //N
  if (serv>5)

 {​​
 sal=sal+(sal*0.1);
 }​​
 if (area=='C')
 {​​
  sal=sal+2500;
 }​​
  if (sales<=25000)
   {
       sal=sal+(sales*0.1);
   }
   else if (sales<50000)
	{​​​​​
	sal=sal+(sales*0.12);
  //I
   printf("Name : %s \n",s_name);
   printf("Your gross monthly remuneration is %f ",sal);
    return 0;
}
